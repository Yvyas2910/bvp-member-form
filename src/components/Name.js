import React from "react";

export const Name = ({name}) => {
  return (
    <>
      <label className="fw-bold">૧. પૂરું નામ :-</label>
      <label className="fw-bold">{name}</label>
    </>
  );
};
